services:
  web:
    build: .
    restart: always
    ports:
      - "127.0.0.1:5000:5000"
    volumes:
      - .:/app
    environment:
      FLASK_ENV: development
      LC_ALL: C.UTF-8
      LANG: C.UTF-8
      PYTHONIOENCODING: utf-8
      DOMAIN_URL: https://ailocal.prits.ru