services:
  web:
    build: .
    restart: always
    ports:
      - "127.0.0.1:5000:5000"
    volumes:
      - .:/app
      - chat_db:/app/chat.db
    environment:
      FLASK_ENV: development
      SECRET_KEY: dev-secret-change-me

volumes:
  chat_db: